import { useState } from "react";

export default Kana = () => {
  const [kana, setKana] = useState("hiragana");

  return;
  <div>
    <h3>Practice Kana</h3>
    <button>{kana}</button>
  </div>;
};
